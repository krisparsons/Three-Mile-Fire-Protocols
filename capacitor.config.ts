import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.threemilefire.sog',
  appName: 'Three Mile Fire SOG',
  webDir: 'dist'
};

export default config;
